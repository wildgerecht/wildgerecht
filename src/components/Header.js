const Header = styled.header`
  /* background: var(--color-black); */
  height: 6rem;
  z-index: 10;
  position: relative;
  overflow: hidden;
`

const Header = ({}) => {
  return (
    <>
      <Wrapper>
        <h1>Header</h1>
      </Wrapper>
    </>
  )
}

export default Header
